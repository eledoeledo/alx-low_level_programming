#include "main.h"

/**
*_abs - Write a function that computes the absolute value of an integer
*@n:The number
*Return: the _abs of number
*/

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

