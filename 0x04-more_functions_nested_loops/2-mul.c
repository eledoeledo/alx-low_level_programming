#include "main.h"

/**
*mul(int a, int b) - function that multiplies two integers
*@a:integer first
*@b:integer two
*Return: Always 0 (Success)
*/

int mul(int a, int b)
{
	int toto = a * b;

	return (0);
}
